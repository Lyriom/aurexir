# Backend en producción (usado por `vite build`).
VITE_API_URL=https://api.aurexir.com

# Clave PUBLICABLE de Stripe (pk_test_… en dev, pk_live_… en prod). Pública; nunca la secreta sk_….
VITE_STRIPE_PUBLISHABLE_KEY=
